# Frontend

Documentation for this project lives in the [root README](../README.md).

**Quick start:** from this directory, run `npm install` and `npm run dev`, then open [http://localhost:3000](http://localhost:3000). The backend API must be running first — see the root README for full setup.
